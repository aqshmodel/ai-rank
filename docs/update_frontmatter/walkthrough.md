# フロントマター（メタデータ）改善の完了報告

すべてのブログ記事（計7件）のフロントマターの見直しと修正が完了しました。
これにより、CTR向上（OGPやファーストビューでの訴求力強化）およびSEOのための構造化データが統一されました。

## 実施した内容

1. **タイトルの13文字最適化**
   長すぎたタイトルを削除し、ガイドラインに沿った短くキャッチーな見出しへ一新しました。
   - `dify-rag-failure-reality.md` → 「**絶望を呼ぶDifyの罠**」
   - `dx-before-team-building.md` → 「**若手が辞める本当の理由**」
   - `end-of-prompt-engineering.md` → 「**プロンプト職人の終焉**」
   - `iwate-ai-consulting.md` → 「**岩手法人向けAIコンサル**」
   - `karpathy-ai-coding-guidelines.md` → 「**AIの暴走を防ぐ4原則**」
   - `local-ai-failure.md` → 「**生成AI導入の失敗法則**」

2. **メタデータの欠落補完**
   - 複数の記事から欠落していた `category` 属性（`AI・DX推進` または `AI組織論・ケーススタディ`）を付与しました。

3. **フォーマット・表記揺れの統一**
   - 日付フォーマットをすべて `YYYY.MM.DD` 形式（例: `2026.04.21`）へ統一しました。
   - `iwate-ai-consulting.md` のカバー画像指定を `.png` からガイドライン推奨の `.webp` へ修正しました。
   - 各ファイルのメタデータの項目の並び順（title, description, coverImage, date, category, tags, author）を美しく整理しました。

## 次のステップ

*   Expressサーバーを再起動またはリロードし、記事一覧や各記事ページ上でのタイトル表示の崩れがないか確認してください。
*   既存の長いタイトルをSNSシェア用に残したい場合は、将来的にテンプレート側を回収して `og:title` 用語のカスタム属性を追加することを検討できます。
